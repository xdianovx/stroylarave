<section class="{{ $class }}">
    <div class="container">
        <div class="wrap">
            <x-ui.text-md text="{{ $title }}" />
            <p class="mt-12 max-w-[420px] mx-auto">{{ $text }}</p>
        </div>
    </div>
</section>
