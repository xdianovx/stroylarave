import { gsap } from "gsap";
import { ScrollTrigger } from "gsap/all";

export const offers_screen = () => {
    // const section = document.querySelector(".offer-scrub-section");
    // if (!section) return;
    // const panels = section.querySelectorAll(".offer-scrub-section-panel");
    // gsap.registerPlugin(ScrollTrigger);
    // ScrollTrigger.create({
    //     trigger: section,
    //     markers: true,
    //     scrub: true,
    //     pin: true,
    // });
};
